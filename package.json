{
    "name": "ccss-support-manual-monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "resolutions": {
        "@types/react": "^16.8.17"
    },
    "scripts": {
        "eslint": "yarn eslint:backend && yarn eslint:common && yarn eslint:frontend",
        "eslint:backend": "cd packages/backend && yarn eslint",
        "eslint:common": "cd packages/common && yarn eslint",
        "eslint:frontend": "cd packages/frontend && yarn eslint",
        "prestart": "yarn tsc",
        "start:backend": "yarn prestart && cd packages/backend && yarn dev",
        "start:frontend": "yarn prestart && cd packages/frontend && yarn start",
        "test": "yarn test:common && yarn test:backend && yarn test:frontend",
        "test:common": "yarn prestart && cd packages/common && yarn test",
        "test:backend": "yarn prestart && cd packages/backend && yarn test",
        "test:frontend": "yarn prestart && cd packages/frontend && yarn test",
        "tsc": "tsc --build",
        "build": "yarn clean && yarn tsc && ts-node scripts/build.ts",
        "build:backend": "yarn tsc && cd packages/backend && yarn build:app",
        "build:frontend": "yarn tsc && cd packages/frontend && yarn build",
        "clean": "ts-node scripts/clean.ts"
    },
    "dependencies": {
        "@typescript-eslint/eslint-plugin": "1.12.0",
        "@typescript-eslint/parser": "1.12.0",
        "rimraf": "^2.6.3"
    },
    "devDependencies": {
        "@types/jest": "^24.0.15",
        "@types/rimraf": "^2.0.2",
        "eslint": "^6.0.1",
        "jest": "^24.8.0",
        "ts-jest": "^24.0.2",
        "ts-node": "^8.3.0",
        "typescript": "^3.5.3"
    }
}