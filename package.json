{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "resolutions": {
        "@types/react": "^16.8.17"
    },
    "scripts": {
        "build": "lerna run build --stream --scope=@ccss-support-manual/{common,backend,frontend}",
        "pretest": "yarn build",
        "test": "jest --config=jest.json"
    }
}