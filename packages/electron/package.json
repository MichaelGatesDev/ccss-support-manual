{
  "name": "electron",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "tsc": "tsc",
    "prepare": "cd ../../ && yarn run clean && yarn run tsc && yarn run build:backend && yarn run build:frontend",
    "start": "electron ./dist/index.js",
    "start:full": "yarn run prepare && yarn run start"
  },
  "dependencies": {
    "@ccss-support-manual/backend": "1.0.0",
    "electron": "^8.1.1"
  },
  "devDependencies": {
    "@types/electron": "^1.6.10"
  }
}
